# 联想电脑产品电商前台系统需求文档

## 1. 项目背景与目标

### 1.1 项目背景

本项目旨在构建一个面向联想电脑产品的电商前台系统，为用户提供联想品牌下各类电脑产品及相关配件的在线浏览、咨询和购买服务。系统采用前后端分离架构，前端基于Web技术实现，后端通过RESTful API提供数据服务。

### 1.2 项目目标

系统应当实现以下核心目标：

- 提供商品展示功能，支持用户浏览联想品牌下各类电脑产品
- 提供用户认证功能，支持用户注册和登录
- 提供购物车功能，支持用户添加商品、调整数量和查看总价
- 提供AI导购功能，为用户提供智能商品推荐服务
- 支持响应式设计，适配桌面端、平板和移动设备

## 2. 系统概述

### 2.1 系统定位

本系统为一个B2C电商前台网站，面向普通消费者，提供联想电脑产品的在线展示和选购服务。系统不直接处理支付和物流，仅实现商品浏览、咨询和购物车管理功能。

### 2.2 技术架构

系统采用前后端分离架构：

- 前端：基于Vue 3框架构建的单页应用（SPA）
- 后端：通过RESTful API提供数据服务
- 通信：使用HTTP协议，JSON格式数据交换
- 认证：采用JWT（JSON Web Token）认证机制

### 2.3 运行环境

**浏览器环境要求**：

- 支持现代浏览器（Chrome、Firefox、Safari、Edge的最新两个版本）

**网络环境要求**：

- 需要稳定的互联网连接
- 后端API地址：`http://ouc.it.srv.thinkpadstore.lighilit.top/`
- API请求超时时间：30秒

## 3. 功能需求

### 3.1 用户认证模块

#### 3.1.1 用户注册

系统应当提供用户注册功能，具体要求如下：

**输入内容**：

- 用户名（必填）
- 邮箱地址（必填）
- 密码（必填）
- 确认密码（必填）

**验证规则**：

- 所有字段均为必填项
- 密码与确认密码必须一致
- 注册信息通过后端API验证

**处理流程**：

1. 用户填写注册信息
2. 系统验证必填字段是否完整
3. 系统验证两次输入的密码是否一致
4. 系统调用后端注册接口（`POST /user/`）
5. 注册成功后自动跳转至登录页面
6. 注册失败时显示错误提示

#### 3.1.2 用户登录

系统应当提供用户登录功能，具体要求如下：

**输入内容**：

- 用户名（必填）
- 密码（必填）

**认证机制**：

- 采用JWT认证方式
- 后端返回访问令牌（access token）和刷新令牌（refresh token）
- 访问令牌用于API请求认证
- 刷新令牌用于获取新的访问令牌

**处理流程**：

1. 用户输入用户名和密码
2. 系统调用后端登录接口（`POST /login/`）
3. 登录成功后，系统应当：
   - 将访问令牌存储至localStorage（键名：`auth_token`）
   - 将刷新令牌存储至localStorage（键名：`refresh_token`）
   - 将用户基本信息存储至localStorage（键名：`user_info`）
   - 跳转至系统首页
4. 登录失败时显示错误提示

#### 3.1.3 访问令牌自动刷新

系统应当实现访问令牌的自动刷新机制：

**触发条件**：

- 当API请求返回401未授权状态码时
- 当前路由不在白名单中（白名单包含：`/`、`/login`、`/register`）

**刷新流程**：

1. 系统检测到401错误
2. 使用刷新令牌调用令牌刷新接口
3. 刷新成功后：
   - 更新localStorage中的访问令牌
   - 使用新的访问令牌重新发送原请求
4. 刷新失败后：
   - 清除所有认证信息
   - 跳转至登录页面

#### 3.1.4 用户退出

系统应当提供退出登录功能：

- 清除localStorage中的所有认证信息
- 清除localStorage中的用户信息
- 跳转至系统首页

#### 3.1.5 登录状态显示

系统应当在全局头部导航区域显示用户登录状态：

- 未登录状态：显示"注册"和"登录"按钮
- 已登录状态：显示"你好, {用户名}"和"退出"按钮

### 3.2 商品展示模块

#### 3.2.1 商品列表展示

系统应当在首页提供商品列表展示功能：

**展示内容**：

- 轮播图区域
- 商品分类导航
- 商品网格列表

**轮播图功能**：

- 支持自动播放，间隔时间为3秒
- 支持手动切换（左右箭头按钮）
- 显示底部指示点，指示当前位置
- 鼠标悬停时暂停自动播放
- 鼠标移出后恢复自动播放

**商品分类**：

系统应当支持以下9个商品分类：

1. 全部商品
2. Lenovo台式机
3. Lenovo笔记本
4. ThinkPad电脑
5. 平板
6. 手机
7. 选件/服务
8. 显示器/IP周边
9. 智能产品

**分类逻辑**：

- 后端数据包含分类字段时，使用后端分类
- 后端数据不包含分类字段时，系统根据商品名称关键词自动识别分类：
  - 包含"斗战者"或"r7000p" → 台式机
  - 包含"笔记本"或"thinkbook" → 笔记本
  - 包含"thinkpad" → ThinkPad电脑
  - 包含"平板"或"pad" → 平板
  - 包含"手机"或"摩托罗拉" → 手机
  - 包含"显示器" → 显示器/IP周边
  - 包含"智能" → 智能产品
  - 其他 → 选件/服务

**商品列表分页**：

- 每页显示12件商品
- 支持翻页操作
- 显示当前页码和总页数

**商品卡片展示**：

- 商品图片
- 商品名称
- 商品描述
- 商品价格
- 鼠标悬停时显示上移和阴影效果

**响应式布局**：

- 桌面端（宽度≥1200px）：每行4个商品
- 平板端（768px≤宽度<1200px）：每行3个商品
- 小屏平板（576px≤宽度<768px）：每行2个商品
- 移动端（宽度<576px）：每行1个商品

#### 3.2.2 商品详情展示

系统应当提供商品详情页面，展示商品的完整信息：

**商品图片展示**：

- 主图预览区域
- 缩略图列表（横向滚动）
- 点击主图可放大显示（黑色半透明遮罩层）
- 点击缩略图切换主图
- 图片加载失败时显示默认图片

**商品基本信息**：

- 商品名称
- 商品数量（库存状态）
- 商品价格（基础价格）
- 商品描述

**商品配置解析**：

- 后端返回的model字段使用"#"分隔配置项
- 系统应当将"#"替换为"/"进行展示
- 示例：`"i7-13700H#32GB内存#1TB SSD"` 转换为 `"i7-13700H/32GB内存/1TB SSD"`

**额外配置选择**：

- 从后端equipments字段动态加载配置选项
- 每个配置选项包含：
  - 配置名称
  - 额外价格
- 用户单选配置项，选中的配置高亮显示
- 系统自动更新商品总价（基础价格 + 额外配置价格）

**数量选择**：

- 支持增加数量（+按钮）
- 支持减少数量（-按钮）
- 数量范围：1-10件
- 实时显示当前选择的数量

**商品操作**：

- 加入购物车按钮
- 立即购买按钮（当前版本显示开发中提示）

### 3.3 购物车模块

#### 3.3.1 购物车展示模式


**侧边栏模式**：
  - 固定定位，从右侧滑入
  - 宽度占屏幕50%
  - 点击遮罩层或关闭按钮可关闭

#### 3.3.2 购物车列表展示

系统应当展示用户已添加到购物车的商品：

**展示内容**：

- 商品图片
- 商品名称
- 商品单价
- 商品数量（支持调整）
- 单项小计价格（单价 × 数量）
- 删除按钮

**数量调整**：

- 点击"+"按钮增加数量（上限10件）
- 点击"-"按钮减少数量（下限1件）
- 数量变更时自动调用API更新后端数据
- 数量变更时自动更新单项小计和总金额

**删除商品**：

- 点击删除按钮时弹出确认对话框
- 确认后调用API删除购物车项
- 删除后自动刷新购物车列表

**空购物车状态**：

- 显示空购物车图标
- 显示"购物车是空的"提示文字
- 提供"继续购物"按钮，点击后跳转至首页

**购物车统计**：

- 实时计算购物车总金额
- 优先使用后端返回的total_price字段
- 若后端未返回total_price，则本地计算（各单项小计之和）

#### 3.3.3 购物车状态同步

系统应当维护全局购物车状态：

- 使用Vue 3的provide/inject机制实现跨组件状态共享
- 全局状态包含购物车商品列表
- 任何组件对购物车的修改应当同步更新全局状态
- 全局状态变更时，所有使用该状态的组件自动更新

#### 3.3.4 购物车入口

系统应当在全局头部导航区域提供购物车入口：

- 显示购物车图标
- 显示"购物车"文字
- 显示购物车内商品数量徽章（红色圆点）
- 点击后打开购物车侧边栏

#### 3.3.5 添加商品到购物车

在商品详情页面，用户应当能够将商品添加至购物车：

**添加流程**：

1. 用户选择额外配置（可选）
2. 用户选择购买数量
3. 系统计算商品总价：（基础价格 + 额外配置价格）× 数量
4. 用户点击"加入购物车"按钮
5. 系统调用后端API（`POST /cart/`），提交以下数据：
   - product：商品ID
   - quantity：购买数量
   - extra_price：额外配置价格
   - total_price：商品总价
6. 添加成功后：
   - 自动打开购物车侧边栏
   - 更新购物车数量徽章
   - 若商品已存在于购物车，后端自动合并数量

### 3.4 AI导购模块

#### 3.4.1 AI导购入口

系统应当在页面右下角提供AI导购入口：

- 固定定位的浮窗按钮
- 显示"AI导购"文字
- 点击后展开AI导购对话窗口

#### 3.4.2 AI导购对话界面

**界面布局**：

- 对话消息展示区域
- 输入框
- 发送按钮
- 关闭按钮

**对话功能**：

- 支持用户输入文本消息
- 按Enter键或点击发送按钮发送消息
- 输入框为空时禁用发送按钮
- 显示用户消息（右侧气泡样式）
- 显示AI回复消息（左侧气泡样式）
- 支持多轮对话，最多保留6条历史消息
- 自动滚动至最新消息

**推荐商品展示**：

当AI返回推荐商品时，系统应当以卡片形式展示：

- 商品名称
- 商品型号
- 商品价格
- 商品库存状态
- 推荐理由
- 商品亮点标签
- 取舍说明
- 点击卡片跳转至商品详情页

#### 3.4.3 过滤参数

用户在AI对话中可设置以下过滤参数（均为可选）：

- 最低预算
- 最高预算
- 候选商品数量（范围1-20，默认8）

#### 3.4.4 状态反馈

系统应当在AI导购过程中提供以下状态提示：

**加载状态**：

- 发送消息后显示"AI思考中…"
- 发送消息期间禁用输入框和发送按钮

**错误提示**：

- HTTP 429状态码：显示"请求过于频繁，请稍后再试"
- HTTP 400状态码：显示"参数错误或服务暂不可用"
- 其他错误：显示"网络异常，请稍后重试"

#### 3.4.5 交互细节

- 按ESC键关闭AI导购窗口
- 点击窗口外的遮罩层关闭窗口（如有）
- 发送消息后自动清空输入框

#### 3.4.6 认证要求

AI导购功能无需用户登录即可使用（匿名访问）：

- API请求不附加认证令牌
- API请求失败时不触发跳转登录页

### 3.5 头部导航模块

#### 3.5.1 导航栏布局

系统应当在所有页面顶部固定显示导航栏：

- Logo区域（左侧）
- 用户认证状态区域（中间）
- 购物车入口（右侧）

#### 3.5.2 Logo区域

- 显示联想Logo图片
- 显示"联想商店"文字
- 点击后跳转至系统首页

#### 3.5.3 用户认证状态区域

**未登录状态**：

- 显示"注册"按钮，点击后跳转至注册页面
- 显示"登录"按钮，点击后跳转至登录页面

**已登录状态**：

- 显示"你好, {用户名}"
- 显示"退出"按钮，点击后执行退出登录操作

#### 3.5.4 购物车入口区域

- 显示购物车图标
- 显示"购物车"文字
- 显示商品数量徽章（红色圆点，显示购物车内商品总数量）
- 点击后打开购物车侧边栏

#### 3.5.5 响应式设计

- 桌面端：完整显示所有元素
- 移动端：缩小图标尺寸和文字大小，保持功能完整

## 4. 关键业务流程

### 4.1 用户注册流程

```
用户访问注册页面
  ↓
填写注册信息（用户名、邮箱、密码、确认密码）
  ↓
系统验证字段完整性
  ↓
系统验证密码一致性
  ↓
提交后端注册接口
  ↓
注册成功 → 自动跳转至登录页面
注册失败 → 显示错误提示
```

### 4.2 用户登录流程

```
用户访问登录页面
  ↓
填写登录信息（用户名、密码）
  ↓
提交后端登录接口
  ↓
登录成功 → 存储访问令牌和刷新令牌 → 跳转至首页
登录失败 → 显示错误提示
```

### 4.3 商品浏览流程

```
用户访问首页
  ↓
浏览轮播图
  ↓
选择商品分类（可选）
  ↓
浏览商品列表
  ↓
点击商品卡片
  ↓
跳转至商品详情页
  ↓
查看商品图片、配置、价格
  ↓
选择额外配置（可选）
  ↓
选择购买数量
```

### 4.4 添加商品到购物车流程

```
在商品详情页
  ↓
选择额外配置（可选）
  ↓
选择购买数量（1-10件）
  ↓
系统计算商品总价 = (基础价格 + 额外配置价格) × 数量
  ↓
点击"加入购物车"按钮
  ↓
提交后端购物车创建接口
  ↓
后端返回更新后的购物车数据
  ↓
自动打开购物车侧边栏
  ↓
更新购物车数量徽章
```

### 4.5 购物车管理流程

```
用户点击头部购物车图标
  ↓
打开购物车侧边栏
  ↓
显示购物车内所有商品
  ↓
用户可执行以下操作：
  - 调整商品数量（+/- 按钮）
  - 删除商品（点击删除按钮并确认）
  ↓
每次操作后调用后端API更新数据
  ↓
自动刷新购物车列表
  ↓
自动更新购物车总金额
```

### 4.6 AI导购流程

```
用户点击右下角"AI导购"按钮
  ↓
展开AI导购对话窗口
  ↓
用户输入需求（可选择设置预算范围）
  ↓
按Enter键或点击发送按钮
  ↓
显示"AI思考中…"加载状态
  ↓
提交后端AI对话接口（携带历史消息）
  ↓
AI返回推荐商品列表
  ↓
以卡片形式展示推荐商品
  ↓
用户查看推荐理由和商品亮点
  ↓
点击推荐商品卡片
  ↓
跳转至商品详情页
  ↓
进行购买操作
```

### 4.7 访问令牌自动刷新流程

```
用户发起需要认证的API请求
  ↓
后端返回401未授权状态码
  ↓
系统检查当前路由是否在白名单
  ↓
不在白名单 → 继续刷新流程
在白名单 → 不处理
  ↓
使用刷新令牌调用令牌刷新接口
  ↓
刷新成功 → 更新访问令牌 → 重新发送原请求
刷新失败 → 清除所有认证信息 → 跳转至登录页面
```

## 5. 非功能需求

### 5.1 性能需求

- 页面首次加载时间应在可接受范围内
- API请求超时时间设置为30秒
- 轮播图切换间隔为3秒
- 图片加载失败时应在100ms内显示默认图片

### 5.2 可用性需求

- 系统应当提供清晰的用户界面
- 所有操作应当提供即时反馈（加载状态、成功提示、错误提示）
- 表单验证应当实时进行
- 错误信息应当明确具体

### 5.3 兼容性需求

**浏览器兼容性**：

- 支持Chrome最新两个版本
- 支持Firefox最新两个版本
- 支持Safari最新两个版本
- 支持Edge最新两个版本
- 不支持Internet Explorer

**设备兼容性**：

- 支持桌面设备（宽度≥1200px）
- 支持平板设备（768px≤宽度<1200px）
- 支持移动设备（宽度<768px）

### 5.4 安全需求

- 用户密码应当通过HTTPS协议传输（由后端实现）
- 访问令牌应当存储在localStorage中
- 访问令牌应当附加至所有需要认证的API请求头（Authorization: Bearer {token}）
- 刷新令牌仅用于获取新的访问令牌
- 退出登录时应当清除所有认证信息
- 访问令牌失效时应当自动使用刷新令牌获取新令牌

### 5.5 可维护性需求

- 前端代码应当遵循ESLint代码规范
- API调用应当统一封装在服务层
- 组件应当采用单一职责原则
- 全局状态应当采用统一的provide/inject机制

## 6. 系统约束与假设

### 6.1 系统约束

1. **后端API约束**：
   - 后端API地址固定为：`http://ouc.it.srv.thinkpadstore.lighilit.top/`
   - 所有API请求使用JSON格式交换数据
   - 后端遵循RESTful API设计规范

2. **数据约束**：
   - 商品数量选择范围：1-10件
   - AI对话历史最多保留6条消息
   - AI推荐商品数量范围：1-20个，默认8个

3. **功能约束**：
   - 系统不实现支付功能
   - 系统不实现订单管理功能
   - 系统不实现用户中心功能
   - 系统不实现商品搜索功能（仅支持分类筛选）

### 6.2 系统假设

1. **后端假设**：
   - 后端API稳定可用
   - 后端返回的数据格式符合预期
   - 后端支持JWT认证机制
   - 后端支持令牌刷新接口

2. **用户假设**：
   - 用户使用现代浏览器访问系统
   - 用户具备基本的电商网站使用经验
   - 用户的网络连接稳定

3. **数据假设**：
   - 后端商品数据可能不包含分类字段，前端根据商品名称自动分类
   - 后端购物车API支持自动合并相同商品的数量
   - 后端AI导购接口支持携带历史消息的多轮对话

## 7. 数据需求

### 7.1 商品数据

系统应当能够处理以下商品数据：

```
商品基本信息：
- id: 商品唯一标识
- name: 商品名称
- description: 商品描述
- price: 基础价格（数字）
- quantity: 商品数量/库存（数字）

商品详细信息：
- model: 商品配置，格式为"配置1#配置2#配置3"
- equipments: 额外配置选项数组，每个包含：
  - name: 配置名称
  - price: 额外价格（数字）

商品图片：
- image: 主图URL
- images: 所有图片URL数组

商品分类：
- category: 商品分类（可能为空，前端自动识别）
```

### 7.2 用户数据

系统应当存储以下用户数据：

```
用户认证信息：
- username: 用户名
- email: 邮箱地址
- password: 密码（注册时提交）

令牌信息：
- access: 访问令牌（JWT）
- refresh: 刷新令牌（JWT）
```

### 7.3 购物车数据

系统应当处理以下购物车数据：

```
购物车项：
- id: 购物车项唯一标识
- product: 关联的商品ID或商品对象
- quantity: 购买数量（1-10）
- extra_price: 额外配置价格（数字）
- total_price: 该项总价（数字）

购物车汇总：
- total_price: 购物车总金额（后端计算）
```

### 7.4 AI导购数据

系统应当处理以下AI导购数据：

```
用户输入：
- message: 用户消息文本（字符串）
- min_price: 最低预算（数字，可选）
- max_price: 最高预算（数字，可选）
- count: 候选商品数量（数字，1-20，默认8）

AI回复：
- reply: AI回复文本（字符串）
- products: 推荐商品数组，每个包含：
  - id: 商品ID
  - name: 商品名称
  - model: 商品型号
  - price: 商品价格
  - quantity: 商品库存
  - reason: 推荐理由
  - highlights: 商品亮点标签数组
  - trade_off: 取舍说明

对话历史：
- role: 消息角色（user/assistant）
- content: 消息内容
```

## 8. 路由结构

系统应当实现以下路由：

| 路由路径 | 页面名称 | 功能说明 |
|---------|---------|---------|
| `/` | 首页 | 商品列表展示、轮播图、分类导航 |
| `/product/:id` | 商品详情页 | 商品详细信息展示、配置选择、加入购物车 |
| `/cart` | 购物车页 | 购物车商品列表、数量调整、删除商品 |
| `/login` | 登录页 | 用户登录 |
| `/register` | 注册页 | 用户注册 |

## 9. API接口需求

### 9.1 用户认证接口

**用户注册**：
- 接口地址：`POST /user/`
- 请求参数：username, email, password
- 响应数据：注册成功的用户信息

**用户登录**：
- 接口地址：`POST /login/`
- 请求参数：username, password
- 响应数据：access（访问令牌）, refresh（刷新令牌）

**令牌刷新**：
- 接口地址：`POST /login/token/refresh/`
- 请求参数：refresh（刷新令牌）
- 响应数据：access（新的访问令牌）

### 9.2 商品接口

**获取商品列表**：
- 接口地址：`GET /product/`
- 响应数据：商品数组

**获取商品详情**：
- 接口地址：`GET /product/{id}/`
- 响应数据：单个商品的完整信息

### 9.3 购物车接口

**获取购物车列表**：
- 接口地址：`GET /cart/`
- 需要认证：是
- 响应数据：购物车商品数组

**添加商品到购物车**：
- 接口地址：`POST /cart/`
- 需要认证：是
- 请求参数：product, quantity, extra_price, total_price
- 响应数据：创建的购物车项

**更新购物车项**：
- 接口地址：`PATCH /cart/{id}/`
- 需要认证：是
- 请求参数：quantity
- 响应数据：更新后的购物车项

**删除购物车项**：
- 接口地址：`DELETE /cart/{id}/`
- 需要认证：是
- 响应数据：删除成功的确认

### 9.4 AI导购接口

**AI对话**：
- 接口地址：`POST /assistant/chat/`
- 需要认证：否
- 请求参数：message, min_price（可选）, max_price（可选）, count（可选）, history（可选）
- 响应数据：reply（AI回复）, products（推荐商品数组）

## 10. 界面设计要求

### 10.1 整体风格

- 界面风格简洁、现代、专业
- 主色调采用联想品牌色（绿色系）
- 使用卡片式布局
- 保留充足的留白空间

### 10.2 交互设计

- 加载状态应当提供加载提示
- 错误状态应当提供明确的错误信息
- 空状态应当提供友好的空状态提示
- 重要操作应当提供确认对话框（如删除商品）

### 10.3 响应式设计

- 桌面端优先设计
- 使用弹性布局和网格布局
- 图片应当自适应容器大小
- 移动端优化触摸操作区域

## 11. 版本信息

**文档版本**：1.0

**编写日期**：2025年12月31日

**适用系统版本**：thinkpadStore-frontend 0.1.0

**编写依据**：基于项目源代码分析，仅包含已实现的功能需求